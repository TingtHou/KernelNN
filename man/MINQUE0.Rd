% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MINQUE_arm_docs.R
\name{MINQUE0}
\alias{MINQUE0}
\title{MINQUE0 Variance Component Estimation (C++)}
\arguments{
\item{KList}{A list of \eqn{n \times n} symmetric kernel (covariance) matrices.}

\item{y}{A numeric vector of length \eqn{n}.}
}
\value{
A list containing:
\itemize{
  \item \code{vcs} A numeric vector of estimated variance components.
  \item \code{it} An integer, currently always 1 (for consistency with IMINQUE).
}
}
\description{
Computes MINQUE(0) estimates of variance components using a list of kernel
matrices and a response vector \code{y}. This is the basic MINQUE estimator
with prior weights fixed at 1.
}
\examples{
\dontrun{
n <- 30
set.seed(1)
y <- rnorm(n)
K1 <- diag(n)
K2 <- tcrossprod(matrix(rnorm(n*5), n, 5))
MINQUE0(list(K1, K2), y)
}

}

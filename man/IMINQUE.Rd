% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MINQUE_cpp_docs.R
\name{IMINQUE}
\alias{IMINQUE}
\title{Iterative MINQUE (IMINQUE) Variance Component Estimation (C++)}
\arguments{
\item{KList}{A list of \eqn{n \times n} symmetric kernel matrices.}

\item{y}{A numeric response vector.}

\item{prior}{Initial prior values (numeric vector). If empty, it defaults
to a vector of ones of length \code{length(KList)}.}

\item{epoch}{Maximum number of iterations (default: \code{100}).}

\item{threshold}{Convergence tolerance for the Euclidean norm of successive
estimates (default: \code{1e-4}).}

\item{echo}{Logical; if \code{TRUE}, prints iteration details.}
}
\value{
A list containing:
\itemize{
  \item \code{vcs} Final variance component estimates.
  \item \code{it} Number of iterations performed.
  \item \code{err} If present (value \code{-1}), indicates failure in the
    underlying \code{MINQUE()} call.
}
}
\description{
Performs iterative MINQUE estimation. At each iteration, the variance
component estimates from \code{MINQUE()} are used as the new priors until
convergence or until reaching \code{epoch} iterations.
}
\examples{
\dontrun{
n <- 40
set.seed(1)
y <- rnorm(n)
K1 <- diag(n)
K2 <- tcrossprod(matrix(rnorm(n*5), n, 5))
IMINQUE(list(K1, K2), y, prior = c(1,1), echo = FALSE)
}

}
